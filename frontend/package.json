{
  "name": "lifegraph-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "LifeGraph front-end property tests (fast-check)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.1.0"
  }
}
